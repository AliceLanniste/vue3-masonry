{
  "name": "vue3-masonry-monorepo",
  "private": "true",
  "scripts": {
    "build": "pnpm build --filter ",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6"
  }
}
